¿Cuál de los algoritmos de ordenación tiene una complejidad igual a
(n*log2*n)?
rta: QuickSort.

Si un algoritmo de ordenación tiene eficiencia logarítmica entonces este
algoritmo es __________ que un algoritmo con eficiencia lineal.
rta: Mejor

El algoritmo de ordenación que aplica la técnica divide y vencerás
corresponde a:
rta: QuickSort.

Para que el algoritmo de ordenación QuickSort tenga un alto desempeño
depende:
rta: De la elección del Pivote.

¿En qué número de línea se encuentra el error? Para que el siguiente
programa cumpla correctamente con la búsqueda lineal en un arreglo.
rta: 36

Utilizando el algoritmo de BÚSQUEDA BINARIA. ¿Cuántas pasadas se
necesita para encontrar el valor 88? Para la lista de abajo:
[8, 13, 17, 26, 44, 56, 88, 97]
rta: 3


¿Qué algoritmo de ordenación se está usando?
Si inicialmente, se tiene la siguiente lista:
[47, 3, 21, 32, 56, 92]
Después de dos pasadas de un algoritmo de ordenación, la lista se ha
quedado dispuesto así:
[3, 21, 47, 32, 56, 92]
rta: Inserción.

Cuando la variable bajo es mayor que la variable alto en
una BÚSQUEDA BINARIA significa que ya no existe vector a buscar la
clave.
rta: Verdadero

Este algoritmo en la primera pasada busca el menor elemento desde v[0] a
v[n - 1] y lo deposita en la posición v[0], luego se busca el menor elemento
desde v[1] a v[n - 1] y lo deposita en la posición v[1] y, así sucesivamente
para las siguientes pasadas del algoritmo.
rta: Seleccion

Si se tiene una lista de 5.000 elementos el número de elementos
examinados en el peor de los casos en una búsqueda binaria seria:
rta: 14

La codificación de la BÚSQUEDA BINARIA sólo se puede realizar de forma
ITERATIVA.
rta: Falsa

Si los datos están almacenados en un archivo, el proceso de ordenación se
llama ordenación: ________________.
rta: externa

En el método de hundimiento para ordenar arreglos, el ciclo interno cumple
la función para controlar el número de __________ por pasada.
rta: comparacion

¿Cuál es el algoritmo de ordenación que mejora el método de Inserción
Directa?
rta: Shell

Para que se aplique correctamente la BÚSQUEDA BINARIA la lista NO tiene
que estar ordenada ya sea de forma ascendente o descendente.
rta: Falso

El algoritmo que se basa en las lecturas sucesivas de la lista a ordenar,
comparando el elemento inferior de la lista con los restantes y efectuando
__________ de posiciones cuando el orden resultante de la comparación no
sea el correcto.
rta: intercambio

En terminología de ordenación el elemento por el cual está ordenado un
conjunto de datos (o se esta buscando) se denomina: _____________.
rta: clave

La eficiencia en una BÚSQUEDA SECUENCIAL es:
rta: n

¿Este programa ordena los valores de un arreglo en orden?
rta: Descendente

La búsqueda __________ compara cada elemento de un arreglo con la clave
de búsqueda.
rta: lineal


