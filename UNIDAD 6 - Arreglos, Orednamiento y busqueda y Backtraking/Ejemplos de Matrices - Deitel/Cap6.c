
































En el programa de abajo. ¿En qué número de línea se encuentra el error?
rta: 7

El proceso de colocar los elementos de un arreglo en orden ascendente o descendente se conoce como .
rta: Ordenamiento

Se puede introducir de manera directa una cadena en un arreglo de
caracteres desde el teclado utilizando scanf y el especiﬁcador de
conversión %c.
rta: Falso

Se debe ﬁnalizar una directiva de preprocesador #deﬁne o #include con un punto y coma
rta: Falso

El estándar indica que un sistema ANSI C debe de soportar por lo menos
subíndices de arreglos.
rta: 12

¿Qué imprime el programa de abajo?
rta: 60, 70, 90, 41, 95, 18, 27, 32

Asignar un valor a una constante simbólica en una instrucción ejecutable,
es un error de sintaxis.
rta: Verdadero

Si existen menos inicializadores que elementos en el arreglo, C inicializa de
forma automática a _______ los elementos restantes.
rta: 0

Debe de utilizar una _____________ para declarar el tamaño de un arreglo
porque el programa se hace más dimensionable.
rta: Constante simbólica.

La referencia al primer elemento del arreglo c es c[ 1 ].
rta: Falso

Los arreglos static se inicializan automáticamente en tiempo de
ejecución.
rta: Falso

Los arreglos y las estructuras son entidades __________ que mantienen el
mismo tamaño durante la ejecución del programa.
rta: estaticas

La ejecución del programa de abajo inicializa los elementos del arreglo n a
una secuencia de 10 elementos. ¿Cuál es el valor para n[ 5 ]?
rta: 5

Los elementos de un arreglo también pueden inicializarse cuando se
declara el arreglo, colocando un signo igual seguido de un par de llaves, { },
que contenga una lista de inicializadores separados por comas.
rta: Verdadero

Definir el tamaño de un arreglo como una constante simbólica hace que los
programas sean más _________________.
rta: escalables

Un arreglo que utilice dos subíndices se conoce como un arreglo de:
         a) Array.
         b) Doble subíndice.
         c) Arreglo unidimensional.
rta: Sólo (b) es correcta.

Cuando se hace un ciclo en torno a un arreglo, el subíndice del arreglo
nunca debe ser menor que _______ y siempre debe ser menor que el
número total de elementos del arreglo (tamaño –1).
rta: 0

Los arreglos son estructuras de datos que consisten en elementos de
datos heterogéneos.
rta: Falso

Todas las cadenas en C, NO termina en un carácter nulo.
rta: Falso

Los caracteres individuales en una cadena almacenada en un arreglo no
pueden ser accesibles de forma directa utilizando la notación de
subíndices.
rta: Falso

