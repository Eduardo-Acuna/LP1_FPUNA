#include <stdio.h>
#include <stdlib.h>

int fibonacci(int n) {
    if (n == 0 || n == 1) {
        return n;
    } else {
        return fibonacci(n - 1) + fibonacci(n - 2);
    }
}












































¿Cómo definimos un arreglo de punteros a enteros cuyo nombre del
arreglo es LP de 5 elementos?
rta=int *LP[5]


Se tiene un Vector A y un Puntero p, como se puede ver en la imagen de
abajo, que apunta el primer elemento de Vector A, es decir, p = A. Es
correcta  forma *p[6]  para acceder al elemento 34?
rta= Falso

Dada la siguiente función de abajo, es correcto afirmar que:
void fun(ubsigned)
rta=Copia la cadena cad1 en la cadena cad2

Dada la representación en memoria de y y ptrY. La instrucción printf( “%d”,
*ptrY ); imprime:
rta=5

El &, u operador de dirección, es un operador ____________ que devuelve la
dirección de su operando.
rta= unario

El operador de dirección & puede únicamente aplicarse a constantes, a
expresiones y a variables declaradas con la clase de almacenamiento
register.
rta=Falso

Cuando escriba programas que dependan del tamaño del tipo de dato y
que se ejecutarán en varios sistemas de computadoras, utilice ___________
para determinar el número de bytes requeridos para almacenar los tipos de
datos
rta=sizeof

Para pasar un elemento de un arreglo en llamada por ____________ deberá
ser pasada la dirección del elemento especifico del arreglo.
rta=referencia.


Un apuntador que se declara void puede ser desreferenciado
rta= Falso

El calificador ___________ puede utilizarse para reforzar el principio del
menor privilegio
rta=const

Un nombre de arreglo es un apuntador __________ que siempre apunta a la
misma dirección en memoria.
rta=constante

Con la siguiente declaración: int x[ ] = {3, 2, 8, 7, 5}, tal que, *p = x. Es
correcto afirmar que:
rta=La instrucción printf("%d", (*x)++); imprime 3.

Dado el siguiente programa en C, tiene un error. ¿En qué número de línea
se haya el error?
rta=25

Al ejecutar aritmética de punteros en un arreglo de caracteres, los
resultados son como en aritmética normal.
rta= Verdadero

Los apuntadores de distintos tipos no pueden ser asignados uno al otro,
sin una operación de conversión.
rta= Falso

El único entero que se le puede asignar a un puntero es cero.
rta= Verdadero

Al proceso de referenciar a un valor a través de un apuntador se le llama:
rta=Indirección.

Los apuntadores son variables cuyos valores son direcciones de
memoria.
rta=Verdadero

Dado el sig
rta=11































